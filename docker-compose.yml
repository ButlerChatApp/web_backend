services:
  app:
    build:
      context: .
    volumes:
      - .:/app
      - air-config:/root/.air
    environment:
      - SERVICE_ACCOUNT_JSON_PATH="firebase_service_account.json"
    ports:
      - "8080:8080"
volumes:
  air-config: